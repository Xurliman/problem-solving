package main

func addBinary(a string, b string) string {
	res := make([]rune, 0)
	aRunes := []rune(a)
	bRunes := []rune(b)
	for _, aRune := range aRunes {
		for _, bRune := range bRunes {
			
		}
	}
}
